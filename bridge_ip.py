# this can be expanded to actually get the ip

ip = '192.168.0.20'

# need to run the arp -a command to get all the ip addresses available

# ping -a each ip until Phillips-hue found

# can also have the option to give an argument of the name